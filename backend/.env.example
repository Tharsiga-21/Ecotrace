# Use your Gemini API Key here (Required for the Express proxy)
GEMINI_API_KEY=your_gemini_api_key_here

# Customize the model if desired (Optional, defaults to gemini-2.0-flash)
GEMINI_MODEL=gemini-2.0-flash

# Port for the proxy server to listen on (Optional, defaults to 3001)
PORT=3001
